/* This example demonstrates how the FaCT compiler does not allow integer
 * division with secret operands, since on many architectures, the timing of the
 * divison instruction depends on its inputs. */
secret int32 div(secret int32 a, secret int32 b) {
    return a / b;
}
